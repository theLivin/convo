/* eslint-disable */
const state = {
  username: "",
};

const getters = {};

const actions = {
  setUsername({ commit }, payload) {
    console.log(payload);
  },
};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
